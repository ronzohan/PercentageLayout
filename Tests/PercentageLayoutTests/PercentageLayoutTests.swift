import XCTest
@testable import PercentageLayout

final class PercentageLayoutTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PercentageLayout().text, "Hello, World!")
    }
}
